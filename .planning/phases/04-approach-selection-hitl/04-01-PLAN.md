---
phase: 04-approach-selection-hitl
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/graph/state.py
  - src/graph/nodes.py
  - src/graph/workflow.py
autonomous: true

must_haves:
  truths:
    - "Graph pauses at approach_selection node after approach_agent completes"
    - "User selection (1, 2, or 3) routes to writer_agent"
    - "User rejection routes back to approach_agent"
    - "Rejected approaches are stored in state"
    - "Approach agent prompt includes rejected approaches"
  artifacts:
    - path: "src/graph/state.py"
      provides: "rejected_approaches field in ArticleState"
      contains: "rejected_approaches"
    - path: "src/graph/nodes.py"
      provides: "approach_selection_node with Command routing"
      exports: ["approach_selection_node"]
    - path: "src/graph/workflow.py"
      provides: "Updated graph with approach_selection node"
      contains: "approach_selection"
  key_links:
    - from: "src/graph/nodes.py"
      to: "src/graph/state.py"
      via: "approach_selection_node uses rejected_approaches field"
      pattern: "rejected_approaches"
    - from: "src/graph/workflow.py"
      to: "src/graph/nodes.py"
      via: "imports and adds approach_selection_node"
      pattern: "approach_selection_node"
---

<objective>
Implement approach selection HITL node with Command-based routing for selection/rejection paths.

Purpose: Enable users to select one of 3 approaches or reject all to regenerate, with rejected approaches tracked to prevent repetition.

Output: Working graph that pauses for selection, routes to writer (selection) or back to approach agent (rejection).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-approach-selection-hitl/04-RESEARCH.md

# Prior phase summary for patterns
@.planning/phases/02-topic-input-hitl/02-01-SUMMARY.md
@.planning/phases/03-approach-agent/03-02-SUMMARY.md

# Source files to modify
@src/graph/state.py
@src/graph/nodes.py
@src/graph/workflow.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rejected_approaches field to ArticleState and create approach_selection_node</name>
  <files>src/graph/state.py, src/graph/nodes.py</files>
  <action>
**In src/graph/state.py:**
Add `rejected_approaches: list[dict] | None` field to ArticleState TypedDict.
- Add docstring explaining: "List of previously rejected approaches. Each is a dict with 'title' and 'metaphor'. Passed to approach agent to avoid repetition."
- Position after selected_approach_index (logical grouping with approach fields)

**In src/graph/nodes.py:**
1. Add imports at top:
   - `from typing import Literal`
   - Update `from langgraph.types import interrupt` to `from langgraph.types import interrupt, Command`

2. Create approach_selection_node function:
   - Signature: `def approach_selection_node(state: ArticleState) -> Command[Literal["writer_agent", "approach_agent"]]:`
   - Build prompt showing 3 approaches with index, title, description truncated to 100 chars, and metaphor
   - Call `interrupt(prompt)` to pause for user input
   - Handle "reject" input: accumulate approaches to rejected_approaches (create NEW list, don't mutate), return `Command(update={"rejected_approaches": rejected}, goto="approach_agent")`
   - Handle selection "1", "2", "3": parse to 0-based index, return `Command(update={"selected_approach_index": index}, goto="writer_agent")`
   - Handle invalid input: treat as rejection (simple approach per research)
   - Add comprehensive docstring explaining Command routing and type annotation requirement

3. Modify approach_agent_node:
   - Read `rejected = state.get("rejected_approaches") or []` at start
   - If rejected is non-empty, append to APPROACH_SYSTEM_PROMPT:
     ```
     "\n\n--- IMPORTANT ---\n"
     "The user has REJECTED the following approaches. Do NOT repeat these. Generate completely DIFFERENT approaches:\n\n"
     For each rejected: f"REJECTED: \"{r['title']}\"\n  - Metaphor was: \"{r['metaphor']}\"\n\n"
     "Create 3 NEW approaches with different titles and metaphors.\n"
     ```
   - Build system_prompt as local variable (don't modify global APPROACH_SYSTEM_PROMPT)

Pattern references from research:
- Command(update=..., goto=...) for atomic state update + routing
- Type annotation `Command[Literal["node_a", "node_b"]]` required for graph validation
- `rejected + approaches` creates new list (no mutation)
  </action>
  <verify>
- `python -c "from src.graph.state import ArticleState; print('rejected_approaches' in ArticleState.__annotations__)"`
- `python -c "from src.graph.nodes import approach_selection_node; print(approach_selection_node.__doc__[:50])"`
  </verify>
  <done>
- ArticleState has rejected_approaches field
- approach_selection_node exists with Command return type
- approach_agent_node includes rejected approaches in prompt when present
  </done>
</task>

<task type="auto">
  <name>Task 2: Update workflow graph with approach_selection node and Command routing</name>
  <files>src/graph/workflow.py</files>
  <action>
**Modify src/graph/workflow.py:**

1. Update imports to include approach_selection_node:
   ```python
   from src.graph.nodes import (
       topic_input_node,
       approach_agent_node,
       approach_selection_node,  # NEW
       writer_agent_node,
       save_output_node,
   )
   ```

2. In build_graph():
   - Add node: `builder.add_node("approach_selection", approach_selection_node)`
   - Change edge: `builder.add_edge("approach_agent", "approach_selection")` (was approach_agent -> writer_agent)
   - REMOVE the edge from approach_agent to writer_agent (Command handles routing from approach_selection)
   - Keep: `builder.add_edge("writer_agent", "save_output")` (unchanged)

3. Update docstring to explain:
   - Command-based routing: approach_selection uses Command(goto=...) so no explicit edge needed
   - The flow is: START -> topic_input -> approach_agent -> approach_selection --(Command)--> writer_agent OR approach_agent
   - Note that approach_selection can loop back to approach_agent

**Critical:** Do NOT add an edge from approach_selection to anything. Command handles the routing.
  </action>
  <verify>
- `python -c "from src.graph import create_compiled_graph; g = create_compiled_graph(); print('approach_selection' in [n for n in g.get_graph().nodes.keys()])"`
- `python -c "from src.graph import create_compiled_graph; g = create_compiled_graph(); print(g.get_graph().draw_ascii())"` (visual inspection)
  </verify>
  <done>
- Graph has approach_selection node
- Edge exists from approach_agent to approach_selection
- No explicit edge from approach_selection (Command routing)
- Graph compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify core functionality with quick smoke test</name>
  <files>None (verification only)</files>
  <action>
Run existing tests to ensure Phase 1-3 functionality is preserved:
```bash
cd /c/Users/Yann/Documents/sources/me/agent-poc-fundations/pedagogical-article-writer
python -m pytest tests/test_graph.py -v -k "not Approach" --tb=short
```

Run Python import checks to verify changes don't break imports:
```bash
python -c "from src.graph import create_compiled_graph; from src.graph.nodes import approach_selection_node; print('All imports OK')"
```

Verify graph structure has correct nodes:
```bash
python -c "
from src.graph import create_compiled_graph
g = create_compiled_graph()
nodes = list(g.get_graph().nodes.keys())
expected = ['topic_input', 'approach_agent', 'approach_selection', 'writer_agent', 'save_output']
for n in expected:
    assert n in nodes, f'Missing node: {n}'
print('All expected nodes present')
"
```

Note: Full integration tests with LLM will be added in Plan 02.
  </action>
  <verify>
- All non-Approach tests pass
- No import errors
- Graph has all 5 expected nodes
  </verify>
  <done>
- Existing Phase 1-3 tests pass (those that don't require LLM)
- Graph compiles with new approach_selection node
- All imports work correctly
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Graph compiles: `python -c "from src.graph import create_compiled_graph; create_compiled_graph()"`
2. State has new field: `python -c "from src.graph.state import ArticleState; assert 'rejected_approaches' in ArticleState.__annotations__"`
3. New node exists: `python -c "from src.graph.nodes import approach_selection_node; print('OK')"`
4. Workflow imports new node: `grep -q "approach_selection_node" src/graph/workflow.py`
5. Unit tests pass: `python -m pytest tests/test_graph.py -v -k "compiles or has_expected" --tb=short`
</verification>

<success_criteria>
- ArticleState has rejected_approaches field
- approach_selection_node exists with correct Command return type
- approach_agent_node includes rejection context in prompt
- Workflow graph includes approach_selection between approach_agent and writer_agent
- Graph compiles without errors
- Existing unit tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-approach-selection-hitl/04-01-SUMMARY.md`
</output>
