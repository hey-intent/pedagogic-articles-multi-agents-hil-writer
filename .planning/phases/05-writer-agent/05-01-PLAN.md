---
phase: 05-writer-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/graph/nodes.py
  - tests/test_graph.py
  - src/main.py
autonomous: true

must_haves:
  truths:
    - "Writer agent receives topic and selected approach from state"
    - "Writer agent produces markdown article in 3Blue1Brown pedagogical style"
    - "Draft is stored in current_draft field for critic evaluation"
    - "Article uses the metaphor from the selected approach"
    - "Article is substantial (800+ words, multiple sections)"
  artifacts:
    - path: "src/graph/nodes.py"
      provides: "LLM-powered writer_agent_node implementation"
      contains: "WRITER_SYSTEM_PROMPT"
    - path: "tests/test_graph.py"
      provides: "TestWriterAgent test class"
      contains: "class TestWriterAgent"
  key_links:
    - from: "src/graph/nodes.py"
      to: "ChatAnthropic"
      via: "model.invoke() in writer_agent_node"
      pattern: "model\\.invoke.*messages"
    - from: "src/graph/nodes.py"
      to: "ArticleState"
      via: "state access"
      pattern: "state\\[.selected_approach_index.\\]"
---

<objective>
Transform the placeholder writer_agent_node into an LLM-powered agent that drafts articles using the selected pedagogical approach.

Purpose: This is the core content generation node - it turns the user's topic and selected teaching approach into an actual educational article written in 3Blue1Brown's accessible, insight-driven style.

Output: Working writer_agent_node with system prompt for 3Blue1Brown style, tests verifying article generation, and updated demo.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-writer-agent/05-RESEARCH.md
@src/graph/nodes.py
@src/graph/state.py
@tests/test_graph.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement LLM-powered writer_agent_node</name>
  <files>src/graph/nodes.py</files>
  <action>
Transform the placeholder writer_agent_node into an LLM-powered implementation:

1. Add WRITER_SYSTEM_PROMPT constant establishing 3Blue1Brown pedagogical style:
   - Build intuition before formalism
   - Use the provided metaphor throughout the article
   - Create "aha!" moments with shift-in-perspective
   - Ground abstractions in familiar experience
   - Be conversational but precise
   - Article structure: engaging title, intriguing intro, progressive body using metaphor, tying-together conclusion
   - Aim for 800-1200 words

2. Implement writer_agent_node function:
   - Extract topic, approaches, and selected_approach_index from state
   - Get the selected approach dict using the index
   - Format approach details (title, description, metaphor, why_effective) for the prompt
   - Create ChatAnthropic model with claude-sonnet-4-20250514 and max_tokens=4096
   - Build messages with system prompt and user content
   - Invoke model and return {"current_draft": response.content}

3. Add comprehensive docstring explaining:
   - This is a simple LLM node (no tools)
   - All context comes from state
   - Returns markdown article in current_draft
   - Will handle revisions in Phase 6 when critic_feedback is present

Follow the established node contract: receive full state, return only fields to update.
  </action>
  <verify>
Run Python import check: `python -c "from src.graph.nodes import writer_agent_node, WRITER_SYSTEM_PROMPT; print('OK')"`

Verify WRITER_SYSTEM_PROMPT contains key principles: `grep -c "intuition\|metaphor\|aha\|conversational" src/graph/nodes.py` (should show multiple matches)
  </verify>
  <done>
writer_agent_node is LLM-powered with WRITER_SYSTEM_PROMPT constant. Node extracts topic and selected approach from state, invokes ChatAnthropic, returns markdown in current_draft.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tests and update demo for writer agent</name>
  <files>tests/test_graph.py, src/main.py</files>
  <action>
1. Add TestWriterAgent class to tests/test_graph.py with 3 integration tests:

   a. test_writer_produces_markdown_draft:
      - Provide topic, select approach 1
      - Verify current_draft is not None
      - Verify length > 500 characters (substantial content)
      - Verify contains markdown heading "#"

   b. test_draft_incorporates_topic:
      - Provide specific topic like "Quantum Computing"
      - Verify draft_lower contains "quantum" (topic referenced)

   c. test_draft_uses_selected_approach:
      - Provide topic, select approach
      - Verify current_draft exists and has substantial length
      - (Metaphor integration is harder to test programmatically, but length and existence confirm generation)

   All tests should:
   - Use @requires_anthropic_api decorator
   - Use unique thread_ids with uuid4() for isolation
   - Follow existing test patterns from TestApproachSelection

2. Update src/main.py to display the generated article:
   - After the selection loop completes (when result.get("current_draft")), print the article
   - Add section header "--- GENERATED ARTICLE ---"
   - Print the full current_draft content
   - Add separator "--- END ARTICLE ---"

Run tests with: `python -m pytest tests/test_graph.py -v --tb=short`
  </action>
  <verify>
Run tests without API key to confirm they skip: `python -m pytest tests/test_graph.py::TestWriterAgent -v` (all 3 should skip)

With API key, run: `ANTHROPIC_API_KEY=... python -m pytest tests/test_graph.py::TestWriterAgent -v --tb=short` (should pass)

Run main.py to see full workflow: `python -m src.main` (if API keys configured)
  </verify>
  <done>
TestWriterAgent class with 3 integration tests added. Tests verify article generation (length, topic reference, markdown format). main.py prints generated article after selection.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Unit tests pass without API keys (existing tests unaffected):
   `python -m pytest tests/test_graph.py -v --ignore-glob="*integration*" 2>&1 | grep -E "passed|skipped"`

2. Full test suite runs (writer tests skip without API key):
   `python -m pytest tests/test_graph.py -v --tb=short`

3. With ANTHROPIC_API_KEY, writer tests pass:
   `python -m pytest tests/test_graph.py::TestWriterAgent -v --tb=short`

4. Code review checklist:
   - [ ] WRITER_SYSTEM_PROMPT defines 3Blue1Brown style principles
   - [ ] writer_agent_node accesses state correctly (topic, approaches, selected_approach_index)
   - [ ] max_tokens=4096 set on ChatAnthropic to avoid truncation
   - [ ] Tests use unique thread_ids with uuid4()
   - [ ] Tests have @requires_anthropic_api decorator
</verification>

<success_criteria>
Phase 5 is complete when:
1. Writer agent receives topic and selected approach from state (verified by tests)
2. Agent produces substantial markdown article (> 500 chars, has headings)
3. Draft is stored in current_draft field (verified by tests)
4. System prompt establishes 3Blue1Brown pedagogical style
5. All new tests pass with API key, skip appropriately without
6. main.py demo shows full workflow including article output
</success_criteria>

<output>
After completion, create `.planning/phases/05-writer-agent/05-01-SUMMARY.md`
</output>
