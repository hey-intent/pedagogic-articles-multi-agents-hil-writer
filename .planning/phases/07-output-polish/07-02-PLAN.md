---
phase: 07-output-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/graph/workflow.py
  - src/graph/nodes/topic_input.py
  - src/graph/nodes/approach_selection.py
  - src/graph/nodes/critic_agent.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Reading workflow.py explains what LangGraph concepts are demonstrated"
    - "Reading topic_input.py explains the interrupt() HITL pattern"
    - "Reading approach_selection.py explains Command routing pattern"
    - "Reading critic_agent.py explains the reflection loop pattern"
    - "Code is readable without IDE - comments provide context for learning"
  artifacts:
    - path: "src/graph/workflow.py"
      provides: "Module docstring with LangGraph concepts overview"
      contains: "Key LangGraph Concepts Demonstrated"
    - path: "src/graph/nodes/topic_input.py"
      provides: "Educational comments on interrupt pattern"
      contains: "LangGraph Pattern"
    - path: "src/graph/nodes/approach_selection.py"
      provides: "Educational comments on Command routing"
      contains: "LangGraph Pattern"
    - path: "src/graph/nodes/critic_agent.py"
      provides: "Educational comments on reflection loop"
      contains: "LangGraph Pattern"
  key_links:
    - from: "workflow.py docstring"
      to: "reader understanding"
      via: "concept explanations"
      pattern: "Key LangGraph Concepts"
---

<objective>
Polish code documentation for educational value.

Purpose: Complete CODE-01 (clear, readable code), CODE-02 (comments explaining LangGraph patterns), and CODE-03 (minimal files) requirements. The codebase becomes a learning reference for LangGraph patterns.

Output: Enhanced module docstrings in workflow.py, educational comments in key node files explaining interrupt(), Command routing, and reflection loop patterns.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-output-polish/07-RESEARCH.md

@src/graph/workflow.py
@src/graph/nodes/topic_input.py
@src/graph/nodes/approach_selection.py
@src/graph/nodes/critic_agent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance workflow.py with comprehensive LangGraph concepts documentation</name>
  <files>src/graph/workflow.py</files>
  <action>
Replace the module docstring in workflow.py with an enhanced version that serves as a learning reference. Keep existing code unchanged - only enhance the module docstring.

The new docstring should include:

1. Module description (what it does)

2. "Key LangGraph Concepts Demonstrated" section explaining:
   - StateGraph: Builder that takes a state schema (TypedDict)
   - START/END: Special constants for entry/exit points
   - Nodes: Functions that receive full state, return partial updates, never mutate
   - Command Routing: Nodes return Command(goto=...) for dynamic flow control
   - InMemorySaver: Checkpointer for HITL and state persistence

3. "Workflow Structure" section with ASCII diagram showing:
   - START -> topic_input(HITL) -> approach_agent -> approach_selection(HITL)
   - approach_selection routes to writer_agent or back to approach_agent
   - writer_agent -> critic_agent -> save_output -> END
   - critic_agent may loop back to writer_agent for revision

4. "See Also" section pointing to state.py, nodes/, and LangGraph docs

Keep the existing function docstrings (build_graph, create_compiled_graph) unchanged - they are already well documented.
  </action>
  <verify>
Check docstring contains key sections: `grep -c "Key LangGraph Concepts" src/graph/workflow.py`
Check ASCII diagram: `grep -c "START ->" src/graph/workflow.py`
Python import check: `python -c "from src.graph.workflow import build_graph"`
  </verify>
  <done>
workflow.py module docstring explains LangGraph concepts and shows workflow structure visually.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add educational pattern comments to HITL and routing nodes</name>
  <files>src/graph/nodes/topic_input.py, src/graph/nodes/approach_selection.py, src/graph/nodes/critic_agent.py</files>
  <action>
Enhance docstrings in key node files to explain the LangGraph patterns they demonstrate. Keep existing code logic unchanged.

1. **topic_input.py** - Already has good interrupt() documentation. Add a header comment block at the start of the docstring:
```
LangGraph Pattern: interrupt() for Human-in-the-Loop
====================================================
```
Keep existing docstring content.

2. **approach_selection.py** - Read the file first, then enhance the function docstring to include:
```
LangGraph Pattern: Command for Dynamic Routing
==============================================
Command(goto="node_name") lets a node control where the graph
goes next, instead of relying on fixed edges. This is powerful
for conditional flows like:
- User selects approach -> go to writer
- User rejects approaches -> go back to approach_agent

The Command return type annotation is REQUIRED:
- `Command[Literal["writer_agent", "approach_agent"]]`
- This tells LangGraph which nodes are valid destinations
- Graph compilation fails if annotation is missing

Note: No explicit edge FROM this node is needed in build_graph()
because Command handles routing dynamically.
```

3. **critic_agent.py** - Read the file first, then enhance the function docstring to include:
```
LangGraph Pattern: Reflection Loop with Command Routing
=======================================================
This node implements the critic side of a writer-critic reflection loop.
The pattern:
1. Evaluate the current draft against quality criteria
2. If approved (both scores >= 7) OR max iterations reached:
   Return Command(goto="save_output") to complete workflow
3. If revision needed:
   Return Command(goto="writer_agent", update={"critic_feedback": feedback})
   The update= parameter atomically sets state before routing

Key insight: Checking iteration limit BEFORE LLM call avoids wasted
API calls when we've already decided to terminate.
```

Do NOT change any functional code - only docstrings and comments.
  </action>
  <verify>
Check pattern headers exist:
- `grep -c "LangGraph Pattern" src/graph/nodes/topic_input.py`
- `grep -c "LangGraph Pattern" src/graph/nodes/approach_selection.py`
- `grep -c "LangGraph Pattern" src/graph/nodes/critic_agent.py`
Python import check: `python -c "from src.graph.nodes import topic_input_node, approach_selection_node, critic_agent_node"`
  </verify>
  <done>
Key node files have educational docstrings explaining interrupt(), Command routing, and reflection loop patterns.
  </done>
</task>

</tasks>

<verification>
1. All imports work: `python -c "from src.graph.workflow import create_compiled_graph; from src.graph.nodes import topic_input_node, approach_selection_node, critic_agent_node"`
2. Pattern documentation exists in each file:
   - `grep "LangGraph Pattern" src/graph/workflow.py src/graph/nodes/topic_input.py src/graph/nodes/approach_selection.py src/graph/nodes/critic_agent.py`
3. Workflow ASCII diagram exists: `grep "START" src/graph/workflow.py`
4. No syntax errors: `python -m py_compile src/graph/workflow.py src/graph/nodes/topic_input.py src/graph/nodes/approach_selection.py src/graph/nodes/critic_agent.py`
</verification>

<success_criteria>
- workflow.py has "Key LangGraph Concepts Demonstrated" section
- workflow.py has ASCII workflow diagram in docstring
- topic_input.py has "LangGraph Pattern: interrupt()" header
- approach_selection.py explains Command routing pattern
- critic_agent.py explains reflection loop pattern
- All files import without errors
- No functional changes to code logic
</success_criteria>

<output>
After completion, create `.planning/phases/07-output-polish/07-02-SUMMARY.md`
</output>
