---
phase: 06-reflection-loop
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/graph/workflow.py
  - tests/test_graph.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Graph routes writer_agent to critic_agent (not directly to save_output)"
    - "Critic routes to writer_agent (revision) or save_output (approved/max iter)"
    - "Iteration counter is visible in final state"
    - "Loop terminates on approval or after 3 iterations"
  artifacts:
    - path: "src/graph/workflow.py"
      provides: "Graph with writer-critic loop"
      contains: "critic_agent"
    - path: "tests/test_graph.py"
      provides: "Reflection loop tests"
      contains: "TestReflectionLoop"
  key_links:
    - from: "src/graph/workflow.py"
      to: "critic_agent_node"
      via: "add_node and edge"
      pattern: "add_node.*critic_agent.*critic_agent_node"
    - from: "src/graph/workflow.py"
      to: "writer_agent -> critic_agent edge"
      via: "add_edge"
      pattern: "add_edge.*writer_agent.*critic_agent"
---

<objective>
Wire the reflection loop in the graph and add comprehensive tests.

Purpose: Complete the writer-critic iteration cycle by updating graph edges and verifying all loop behaviors.
Output: Graph with critic_agent node wired, tests for loop termination, iteration counting, and approval routing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-reflection-loop/06-RESEARCH.md
@.planning/phases/06-reflection-loop/06-01-SUMMARY.md
@src/graph/workflow.py
@tests/test_graph.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire critic_agent into workflow graph</name>
  <files>src/graph/workflow.py</files>
  <action>
    1. Add import for critic_agent_node:
       - Update import from src.graph.nodes to include `critic_agent_node`

    2. Add critic_agent node to the graph:
       - Add: `builder.add_node("critic_agent", critic_agent_node)`

    3. Update edges for reflection loop:
       - CHANGE: `builder.add_edge("writer_agent", "save_output")`
       - TO: `builder.add_edge("writer_agent", "critic_agent")`
       - NOTE: No edge FROM critic_agent - Command handles routing dynamically!
         - critic returns Command(goto="writer_agent") for revision
         - critic returns Command(goto="save_output") for approval/max iterations

    4. Update docstring and comments:
       - Update flow diagram in build_graph docstring to show:
         ```
         START -> topic_input -> approach_agent -> approach_selection --(Command)--> writer_agent -> critic_agent --(Command)--> save_output -> END
                                                          |                              ^                |
                                                          +--- (reject) ----------------+                 |
                                                                  via approach_agent                      |
                                                                                                          v
                                                                                        (revise) --> writer_agent
         ```
       - Add comment explaining Command-based routing from critic_agent
       - Note: No edge FROM critic_agent, similar to approach_selection pattern

    The pattern follows approach_selection_node: Command controls routing, no explicit edges FROM the node.
  </action>
  <verify>
    Run: `python -c "from src.graph import create_compiled_graph; g = create_compiled_graph(); print(list(g.get_graph().nodes.keys()))"`
    Verify: Output includes 'critic_agent'
    Run: `python -c "from src.graph import create_compiled_graph; g = create_compiled_graph(); print('Graph compiles')"`
    Verify: No compilation errors
  </verify>
  <done>
    Graph includes critic_agent node.
    writer_agent routes to critic_agent (not save_output).
    critic_agent uses Command for routing (no explicit outgoing edge).
    Flow diagram in docstring updated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add reflection loop tests</name>
  <files>tests/test_graph.py</files>
  <action>
    Add new test class `TestReflectionLoop` with these tests:

    1. `test_graph_has_critic_agent_node(self, graph)`:
       - Verify "critic_agent" in graph node names
       - No API key needed (structure test)

    2. `test_writer_routes_to_critic(self, graph)` - @requires_api_key:
       - Create config with unique thread_id (use uuid4)
       - Run through: invoke({}) -> resume(topic) -> resume("1" to select approach)
       - Verify graph completes (no __interrupt__ in result)
       - Verify "revision_count" in result (LOOP-04 visibility)

    3. `test_iteration_counter_visible_in_state(self, graph)` - @requires_api_key:
       - Run full flow through selection
       - Assert "revision_count" in result
       - Assert result["revision_count"] >= 0

    4. `test_loop_terminates_on_approval(self, graph)` - @requires_api_key:
       - Run full flow through selection
       - Assert result["is_approved"] is True (LOOP-02)
       - Assert result["current_draft"] is not None
       - This verifies the happy path: critic approves, loop exits

    5. `test_max_iterations_enforces_limit(self, graph)` - @requires_api_key:
       - Run full flow through selection
       - Assert result["is_approved"] is True
       - Assert result["revision_count"] <= 3 (LOOP-03)
       - Note: Most articles will be approved within 3 iterations, so we verify the cap exists

    6. `test_state_includes_critic_feedback_field(self, graph)` - @requires_api_key:
       - Run full flow through selection
       - Check that "critic_feedback" key exists in result (may be None if approved first try)
       - This verifies state schema is correct

    All integration tests use:
    - `config = {"configurable": {"thread_id": f"test-{test_name}-{uuid4()}"}}`
    - Three invokes: initial (hits topic interrupt), topic resume (hits selection interrupt), selection resume (runs through critic)

    Import uuid4 at top if not already imported.
    Follow existing test patterns in the file.
  </action>
  <verify>
    Run: `uv run pytest tests/test_graph.py::TestReflectionLoop -v --tb=short`
    Verify: All tests pass (structure tests always, integration tests if API key set)
    If API key not set: structure test should pass, integration tests should skip
  </verify>
  <done>
    TestReflectionLoop class with 6 tests covering:
    - Node existence (structure)
    - Writer-to-critic routing
    - Iteration counter visibility (LOOP-04)
    - Loop termination on approval (LOOP-02)
    - Max iteration enforcement (LOOP-03)
    - State schema correctness
  </done>
</task>

</tasks>

<verification>
All tasks complete when:
- `python -c "from src.graph import create_compiled_graph; g = create_compiled_graph(); nodes = list(g.get_graph().nodes.keys()); assert 'critic_agent' in nodes; print('Graph OK')"` succeeds
- `uv run pytest tests/test_graph.py::TestReflectionLoop::test_graph_has_critic_agent_node -v` passes
- Integration tests pass when OPENROUTER_API_KEY is set
</verification>

<success_criteria>
1. Graph includes critic_agent node
2. writer_agent routes to critic_agent (verified by test_writer_routes_to_critic)
3. revision_count visible in final state (LOOP-04)
4. Loop terminates on approval (LOOP-02, verified by test_loop_terminates_on_approval)
5. Max iterations capped at 3 (LOOP-03, verified by test_max_iterations_enforces_limit)
6. All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-reflection-loop/06-02-SUMMARY.md`
</output>
