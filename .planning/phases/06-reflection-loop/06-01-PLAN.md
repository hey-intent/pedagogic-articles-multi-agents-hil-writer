---
phase: 06-reflection-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/schemas/critic.py
  - src/schemas/__init__.py
  - src/graph/state.py
  - src/graph/nodes/critic_agent.py
  - src/graph/nodes/writer_agent.py
  - src/graph/nodes/__init__.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Critic agent evaluates drafts for accuracy AND comprehensibility"
    - "Critic returns structured evaluation with scores and feedback"
    - "Writer can operate in initial draft mode OR revision mode"
    - "State includes critic_feedback field for passing feedback to writer"
  artifacts:
    - path: "src/schemas/critic.py"
      provides: "CriticEvaluation Pydantic model"
      exports: ["CriticEvaluation"]
    - path: "src/graph/nodes/critic_agent.py"
      provides: "Critic agent with Command routing"
      exports: ["critic_agent_node", "CRITIC_SYSTEM_PROMPT", "MAX_ITERATIONS"]
    - path: "src/graph/nodes/writer_agent.py"
      provides: "Writer with revision mode support"
      contains: "critic_feedback"
    - path: "src/graph/state.py"
      provides: "State schema with critic_feedback"
      contains: "critic_feedback"
  key_links:
    - from: "src/graph/nodes/critic_agent.py"
      to: "src/schemas/critic.py"
      via: "import CriticEvaluation"
      pattern: "from src\\.schemas import CriticEvaluation"
    - from: "src/graph/nodes/writer_agent.py"
      to: "state.get('critic_feedback')"
      via: "checks for revision mode"
      pattern: "critic_feedback.*=.*state\\.get"
---

<objective>
Create critic agent node and writer revision support for the reflection loop.

Purpose: Implement the core agents needed for the writer-critic iteration cycle (AGNT-05, AGNT-06, LOOP-01).
Output: Critic agent with Command routing, writer with revision mode, CriticEvaluation schema, updated state.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-reflection-loop/06-RESEARCH.md
@src/graph/state.py
@src/graph/nodes/writer_agent.py
@src/schemas/__init__.py
@src/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CriticEvaluation schema and update state</name>
  <files>
    src/schemas/critic.py
    src/schemas/__init__.py
    src/graph/state.py
  </files>
  <action>
    1. Create `src/schemas/critic.py` with Pydantic model:
       - `CriticEvaluation` class with fields:
         - `accuracy_score: int` (Field ge=1, le=10, description about technical correctness)
         - `accuracy_issues: str` (Field description: specific issues, empty if score >= 7)
         - `comprehensibility_score: int` (Field ge=1, le=10, description about beginner accessibility)
         - `comprehensibility_issues: str` (Field description: specific issues, empty if score >= 7)
         - `approved: bool` (Field description: True ONLY if BOTH scores >= 7)
         - `feedback: str` (Field description: combined actionable feedback, empty string if approved)
       - Add docstring explaining the dual-criteria evaluation (AGNT-05)

    2. Update `src/schemas/__init__.py`:
       - Import `CriticEvaluation` from `src.schemas.critic`
       - Add to `__all__` list

    3. Update `src/graph/state.py`:
       - Add `critic_feedback: str | None` field to ArticleState
       - Update docstring to document Phase 6 addition
       - Note: `is_approved` and `revision_count` already exist, just add docstring clarification
  </action>
  <verify>
    Run: `python -c "from src.schemas import CriticEvaluation; from src.graph.state import ArticleState; print('Imports OK')"`
    Verify: No import errors, CriticEvaluation and ArticleState are accessible
  </verify>
  <done>
    CriticEvaluation schema validates critic responses.
    ArticleState includes critic_feedback field.
    All imports work without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create critic_agent_node with Command routing</name>
  <files>
    src/graph/nodes/critic_agent.py
    src/graph/nodes/__init__.py
  </files>
  <action>
    1. Create `src/graph/nodes/critic_agent.py`:
       - Define `MAX_ITERATIONS = 3` constant
       - Define `CRITIC_SYSTEM_PROMPT` with:
         - Evaluation criteria for ACCURACY (score 1-10, what each score means)
         - Evaluation criteria for COMPREHENSIBILITY (score 1-10, what each score means)
         - Approval decision rules (BOTH scores >= 7 to approve)
         - Feedback guidelines (specific issues, quotes, suggestions)
         - Use the prompt from 06-RESEARCH.md as reference

       - Define `critic_agent_node(state: ArticleState) -> Command[Literal["writer_agent", "save_output"]]`:
         - Extract `current_draft` and `revision_count` from state
         - FIRST: Check if `revision_count >= MAX_ITERATIONS`, if so return `Command(update={"is_approved": True}, goto="save_output")` to force termination (LOOP-03)
         - Create model with `ChatOpenAI(model=REASONING_MODEL, base_url=..., api_key=...)`
         - Use `model.with_structured_output(CriticEvaluation)` for structured output
         - Build messages with system prompt and user content containing the draft
         - Invoke model to get evaluation
         - If `evaluation.approved`:
           - Return `Command(update={"is_approved": True, "critic_feedback": None}, goto="save_output")`
         - Else:
           - Return `Command(update={"is_approved": False, "critic_feedback": evaluation.feedback, "revision_count": revision_count + 1}, goto="writer_agent")` (LOOP-04)

       - Add comprehensive docstring explaining termination conditions (LOOP-02, LOOP-03)
       - Include type annotation for Command return (REQUIRED for graph validation)

    2. Update `src/graph/nodes/__init__.py`:
       - Import `critic_agent_node`, `CRITIC_SYSTEM_PROMPT`, `MAX_ITERATIONS` from critic_agent
       - Add to `__all__` list

    Follow pattern from approach_selection_node for Command usage.
  </action>
  <verify>
    Run: `python -c "from src.graph.nodes import critic_agent_node, CRITIC_SYSTEM_PROMPT, MAX_ITERATIONS; print(f'MAX_ITERATIONS={MAX_ITERATIONS}')"`
    Verify: MAX_ITERATIONS=3, imports work
  </verify>
  <done>
    critic_agent_node evaluates drafts using structured output.
    Returns Command routing to writer_agent (revision) or save_output (approved/max iterations).
    MAX_ITERATIONS=3 enforced (LOOP-03).
    revision_count incremented on rejection (LOOP-04).
    CRITIC_SYSTEM_PROMPT documents clear evaluation criteria (AGNT-06).
  </done>
</task>

<task type="auto">
  <name>Task 3: Add revision mode to writer_agent_node</name>
  <files>src/graph/nodes/writer_agent.py</files>
  <action>
    Modify `writer_agent_node` to support two modes:

    1. After extracting topic, approaches, selected_idx, approach from state:
       - Add: `critic_feedback = state.get("critic_feedback")`
       - Add: `current_draft = state.get("current_draft")`

    2. Add mode detection and branching:
       - If `critic_feedback and current_draft`:
         - REVISION MODE: Build user_content with:
           - Current article (current_draft)
           - Critic feedback to address
           - Original context (topic, approach title, metaphor)
           - Instructions to: address ALL feedback points, maintain pedagogical approach, keep 3Blue1Brown style, provide COMPLETE revised article
       - Else:
         - INITIAL DRAFT MODE: Keep existing logic unchanged

    3. Update docstring:
       - Explain the two modes (initial draft vs revision)
       - Note that mode is detected by checking critic_feedback presence
       - Remove "In Phase 6" comment since this IS Phase 6

    Reference the revision prompt pattern from 06-RESEARCH.md.
  </action>
  <verify>
    Run: `python -c "from src.graph.nodes import writer_agent_node; print('Import OK')"`
    Verify: No import errors
    Manual check: Read the file and confirm revision mode logic is present
  </verify>
  <done>
    writer_agent_node handles both initial drafts and revisions.
    Revision mode detected by presence of critic_feedback in state.
    Revision prompt includes current draft, feedback, and instructions to address all points.
  </done>
</task>

</tasks>

<verification>
All tasks complete when:
- `python -c "from src.schemas import CriticEvaluation; from src.graph.nodes import critic_agent_node, CRITIC_SYSTEM_PROMPT, MAX_ITERATIONS, writer_agent_node; print('All imports OK')"` succeeds
- CriticEvaluation has accuracy_score, comprehensibility_score, approved, feedback fields
- critic_agent_node has Command return type annotation
- writer_agent_node checks for critic_feedback to determine mode
- MAX_ITERATIONS == 3
</verification>

<success_criteria>
1. CriticEvaluation schema with dual-criteria evaluation (AGNT-05)
2. critic_agent_node with Command routing (LOOP-01, LOOP-02)
3. Max iteration check BEFORE LLM call (LOOP-03)
4. revision_count incremented in Command update (LOOP-04)
5. Writer revision mode integrates critic feedback
6. Clear, documented prompts (AGNT-06)
</success_criteria>

<output>
After completion, create `.planning/phases/06-reflection-loop/06-01-SUMMARY.md`
</output>
